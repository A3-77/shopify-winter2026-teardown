# 04 · 几何全量统计与最终技术架构

> 本文补完第五阶段遗留项（其余场景的 GLB 几何统计），并给出第十二 / 十三阶段要求的
> **最终技术架构图**与**实现顺序**。
>
> 证据文件：`evidence/GLB_meta.json`（37 个 GLB 的完整元数据）、`evidence/GLB_urls.txt`
> 采集脚本：`tools/fetch_glb_meta.py`

---

## 0. 一句话结论

**全站 3D 内容 = 20 个"角色/道具"模型 + 7 个"背景平面" + 若干迭代残留版本，合计 87 万顶点、88 万三角面、17.9 MB。**
其中 **19 个模型用的是 unlit（无光照）材质** —— 这不是一个靠 PBR 光照撑起来的站，而是一个
**"把插画当贴图、用相机运动制造纵深"** 的站。这解释了为什么整站视觉更接近"立体书"而不是"3D 场景"。

---

## 1. 采集方法（为什么不用下载完整模型）

GLB 的二进制布局是固定的：

```
[ 12B header: magic | version | totalLength ]
[  8B chunk0 header: length | type='JSON' ]
[     JSON chunk —— 场景图、mesh、accessor、材质、扩展声明全在这里 ]
[  8B chunk1 header: length | type='BIN\0' ]
[     BIN chunk —— 顶点/索引的实际字节 ]
```

关键点：**每个 accessor 的 `count` 写在 JSON chunk 里**，即使几何被 Draco 压缩也不例外
（`KHR_draco_mesh_compression` 只替换 BIN 里的数据，`attributes.POSITION` 指向的仍是原始 accessor）。

所以只要 **Range 请求前 256 KB** 就能拿到全部几何元数据 ——
37 个模型合计只下载了约 9 MB，而不是 17.9 MB，且**完全不需要解码 Draco**。

> 环境笔记：Shopify 的 3D CDN（`editions-winter-2026.myshopify.com/cdn/shop/3d/models/`）
> **拒绝不带 User-Agent 的 HEAD 请求**（Python `urllib` 默认 UA 会被拒），
> 但带 UA 的 GET + `Range` 正常返回 `206 Partial Content` 与 `Content-Range`（含文件总长）。

---

## 2. 全量统计（37 个，按体积降序）

| 大小 | 顶点 | 三角面 | 网格 | 材质 | 骨骼 | 动画 | 文件 |
|---:|---:|---:|---:|---:|---:|---:|---|
| 908K | 48,384 | 46,793 | 7 | 5 | 66 | 1 | `EW26_Finance_251208v2` |
| 888K | 36,125 | 49,089 | 15 | 3 | **170** | 1 | `EW26_Hero_251207v3` |
| 860K | 3,124 | 3,702 | 4 | 3 | 0 | 1 | `POS_V53_environment_251209v2` |
| 834K | 46,271 | 43,679 | 7 | 4 | 66 | 1 | `finance_fg_smaller` |
| 826K | 36,126 | 49,089 | 15 | 3 | **170** | 1 | `EW26_Hero_251205` |
| 766K | 55,678 | 43,097 | 9 | 3 | 111 | 1 | `B2B_fg_smaller_v2` |
| 711K | 35,379 | 39,188 | 13 | 10 | 58 | 1 | `Retail_mg_251205v2` |
| 694K | **144,040** | 65,780 | 6 | 1 | 20 | **6** | `Operations_fg_smaller` |
| 677K | 26,652 | 26,269 | 6 | 3 | 33 | 4 | `EW26_Online_251209v3` |
| 673K | 29,508 | 49,655 | 9 | 2 | 46 | 3 | `EW26_Shipping_251204` |
| 657K | 14,458 | 22,728 | **29** | 2 | 34 | 1 | `EW26_Developer_251207v5` |
| 656K | 14,458 | 22,728 | 29 | 2 | 34 | 1 | `EW26_Developer_251128` |
| 650K | 20,408 | 24,276 | 10 | 4 | 111 | 1 | `EW26_B2B_251205v2` |
| 627K | 29,603 | 28,903 | 7 | 3 | 33 | 1 | `Online_fg_smaller` |
| 580K | 127,635 | 49,732 | 2 | 2 | 0 | 0 | `Marketing_fg_smaller` |
| 528K | 9,762 | 17,132 | 5 | 2 | 31 | 4 | `Checkout_fg_smaller` |
| 525K | 12,360 | 18,277 | 4 | 2 | 36 | 2 | `Shipping_fg_smaller` |
| 518K | 9,386 | 16,336 | 7 | 3 | 31 | 4 | `EW26_Checkout_251209` |
| 480K | 9,940 | 18,148 | 6 | 3 | 53 | 5 | `EW26_ShopApp_251208v3` |
| 474K | 17,127 | 26,114 | 7 | 2 | 0 | 5 | `EW26_Sidekick_251208` |
| 472K | 16,807 | 32,932 | 6 | 1 | 20 | 6 | `EW26_Operations_251207` |
| 467K | 17,994 | 30,317 | 7 | 2 | 0 | 5 | `EW26_Sidekick_251207v4` |
| 450K | 8,882 | 16,065 | 6 | 2 | 53 | 5 | `EW26_ShopApp_251128v2` |
| 429K | 17,816 | 31,100 | 2 | 1 | 0 | 0 | `EW26_Marketing_251209v4` |
| 399K | **4** | **2** | 1 | 1 | 0 | 0 | `Checkout_bg_diffuse` |
| 356K | **4** | **2** | 1 | 1 | 0 | 0 | `Shipping_bg_diffuse` |
| 324K | **4** | **2** | 1 | 1 | 0 | 0 | `Developer_bg_diffuse` |
| 324K | 4,822 | 4,162 | 1 | 1 | 0 | 0 | `key.glb` |
| 299K | 42,037 | 36,736 | 7 | 7 | 0 | 1 | `POS_v53_Hub_251208v2` |
| 278K | 25,493 | 50,247 | 1 | 1 | 0 | 0 | `Agentic_fg_251209` |
| 240K | **4** | **2** | 1 | 1 | 0 | 0 | `EW26_Sidekick_bg_stars` |
| 163K | 6,984 | 10,828 | 9 | 3 | 0 | 1 | `EW26_Agentic_Props_251209v5` |
| 162K | **4** | **2** | 1 | 1 | 0 | 0 | `ShopApp_bg_diffuse` |
| 135K | **4** | **2** | 1 | 1 | 0 | 0 | `Finance_bg_diffuse` |
| 122K | **4** | **2** | 1 | 1 | 0 | 0 | `Operations_bg_diffuse` |
| 77K | 3,829 | 7,080 | 1 | 1 | 0 | 0 | `Renaissance_Globe_251209v2` |
| 53K | 2,300 | 3,616 | 7 | 1 | 90 | 1 | `Rigged_Book_CS_Animated_V5` |

**汇总**

| 指标 | 数值 |
|---|---|
| 顶点合计 | **873,416** |
| 三角面合计 | **883,812** |
| 体积合计 | **17.9 MB** |
| Draco 压缩 | **30 / 37** |
| 带骨骼蒙皮 | **20 / 37** |
| 带动画 | **25 / 37** |

**glTF 扩展使用**

| 次数 | 扩展 | 含义 |
|---:|---|---|
| 36 | `KHR_texture_basisu` | 贴图用 KTX2 / Basis Universal 超压缩 |
| 30 | `KHR_draco_mesh_compression` | 几何用 Draco 压缩 |
| **19** | `KHR_materials_unlit` | **无光照材质 —— 直接输出贴图色** |
| 2 | `KHR_materials_ior` | 折射率（玻璃/水类材质） |
| 2 | `KHR_materials_specular` | 镜面反射控制 |

---

## 3. 四个关键发现

### 3.1 【已确认】背景层就是一块 quad —— 2.5D 分层被彻底坐实

7 个 `*_bg_diffuse` / `bg_stars` 模型是 **4 个顶点、2 个三角面、1 个网格、1 个材质**。

```
Checkout_bg_diffuse    399K   4 顶点
Shipping_bg_diffuse    356K   4 顶点
Developer_bg_diffuse   324K   4 顶点
ShopApp_bg_diffuse     162K   4 顶点
Finance_bg_diffuse     135K   4 顶点
Operations_bg_diffuse  122K   4 顶点
EW26_Sidekick_bg_stars 240K   4 顶点
```

一个 4 顶点的网格就是一个矩形平面。它们体积 122K–399K，说明 **KTX2 贴图直接内嵌在 GLB 里**。

这直接回答了两个问题：
- **背景不是 CSS 图片，也不是 `TextureLoader` 加载的裸贴图，而是"贴图平面"以 GLB 形式下发。**
  好处是贴图与几何、材质、UV 打包在一次请求里，且能被 Draco/Basis 管线统一处理。
- **我之前"没有 depth map"的结论得到二次确认** —— 背景是实打实的平面几何，
  靠相机运动产生纵深，不是单张图做视差位移。

### 3.2 【已确认】Hero 人物是 170 根骨骼的蒙皮网格

`EW26_Hero_251207v3`：36,125 顶点 / 49,089 三角面 / 15 个网格 / **170 根骨骼** / 1 段动画。

这与我在运行时用 three devtools 钩子抓到的场景图完全吻合 ——
`Robed_Woman_Rig` 与 `Robed_Man_Rig` 是 `SkinnedMesh`，含 `skinIndex` / `skinWeight`。

**170 根骨骼对两个角色来说相当密集**（通常双角色绑定在 80–120 根）。这说明手指、衣摆、
甚至兜帽的布料摆动都是骨骼驱动，而不是顶点动画或形态键。

其他高骨骼数模型：`B2B_fg` 111 根、`Rigged_Book` 90 根、`EW26_Finance` / `finance_fg` 各 66 根。

### 3.3 【已确认】近一半模型用 unlit 材质 —— 这个站不靠光照

**19 / 37 个模型声明了 `KHR_materials_unlit`。**

unlit 意味着：材质不参与任何光照计算，直接输出 `baseColor × texture`。
配合运行时实测到的 `shadowMap: false`（阴影贴图关闭），可以确认：

> **整站没有做 PBR 光照。** 立体感来自三处：
> ① 贴图本身是画好的插画（美术已烘焙好明暗）；② 相机运动带来的透视变化；③ 后处理（bloom / 色调）。
>
> 这是"**把插画切层 + 3D 相机运动**"的做法，不是"建 3D 场景打光渲染"。
> 这也解释了为什么全站 87 万顶点的体积感依然接近 2D —— 因为它本质上是 2D 的。

### 3.4 【高度可能】37 个 GLB 里有相当一部分是迭代残留

按文件名日期能看出成对出现的版本：

| 场景 | 版本 A | 版本 B |
|---|---|---|
| Hero | `EW26_Hero_251205` | `EW26_Hero_251207v3` |
| Sidekick | `EW26_Sidekick_251207v4` | `EW26_Sidekick_251208` |
| Developer | `EW26_Developer_251128` | `EW26_Developer_251207v5` |
| ShopApp | `EW26_ShopApp_251128v2` | `EW26_ShopApp_251208v3` |

两两之间几何数据完全一致（如 Developer 两个版本都是 14,458 顶点 / 22,728 三角面 / 29 网格 / 34 骨骼），
**只有文件 hash 与体积略有差异**。这说明它们是同一模型的不同压缩批次或不同构建产物，
**CDN 上保留了历史版本**。

> 【诚实边界】我无法确认线上实际请求的是哪一份 —— 这需要抓真实运行时的网络请求。
> 可以确定的是：**CDN 上的 37 个 ≠ 线上在用的 37 个**。
> 按 13 章 × (背景平面 + 主体 + 前景) 估算，实际在用的约 20–24 个。

---

## 4. 最终技术架构图

### 4.1 资源层 → 运行时层 → 输出层

```
┌─────────────────────────────── 资源层（CMS / CDN 下发，前端零硬编码）───────────────────────────────┐
│                                                                                                   │
│  sections JSON            {Scene}.theatre-project-state.json         3D 资产                       │
│  ┌──────────────┐         ┌──────────────────────────┐              ┌─────────────────────────┐   │
│  │ 章节顺序      │         │ 82 对象 / 375 轨道        │              │ 37 GLB   (17.9MB)       │   │
│  │ 高度 / 配色   │         │ 780 关键帧               │              │  30 个 Draco 压缩        │   │
│  │ 1247 个资产URL│         │ 相机 xyz/fov/target      │              │  19 个 unlit 材质        │   │
│  │ 文案 / 图片   │         │ 每章 1 份                │              │  KTX2/Basis 贴图内嵌     │   │
│  └──────┬───────┘         └────────────┬─────────────┘              └────────────┬────────────┘   │
└─────────┼──────────────────────────────┼──────────────────────────────────────────┼───────────────┘
          │                              │                                          │
          ▼                              ▼                                          ▼
┌─────────────────────────────── 运行时层（Remix/Hydrogen on Oxygen，SSR + 流式 hydration）─────────┐
│                                                                                                   │
│  ┌────────────────┐      ┌──────────────────────┐      ┌────────────────────────────────────┐   │
│  │  Lenis 1.3.23  │      │  zustand 风格 store   │      │  Theatre.js (@theatre/core)        │   │
│  │  平滑滚动       │─────▶│  scrollY / sectionMap │─────▶│  sequence.position =              │   │
│  │  lenis.scroll  │      │  activeSection        │      │    max(0, p·(h+vh)/vh + offset)   │   │
│  └────────────────┘      └──────────┬───────────┘      └─────────────┬──────────────────────┘   │
│                                     │                                │                          │
│                          ┌──────────▼───────────┐         ┌──────────▼──────────┐              │
│                          │ computeSectionProgress│         │ 轨道求值 → 每帧属性  │              │
│                          │  区间起点 = acc-vh-ec │         │ 相机/图层 transform │              │
│                          │  同时算 current+next  │         └──────────┬──────────┘              │
│                          └──────────┬───────────┘                    │                          │
│                                     │                                │                          │
│                                     ▼                                ▼                          │
│                     ┌───────────────────────────────────────────────────────────┐                │
│                     │        @react-three/fiber  <Canvas>  (sticky 钉住)         │                │
│                     │  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  │                │
│                     │  │ Scene i       │  │ Scene j       │  │ 共用资源       │  │                │
│                     │  │ bg/mid/fg 三层│  │ bg/mid/fg 三层│  │ KTX2/Draco    │  │                │
│                     │  │ + 独立相机     │  │ + 独立相机     │  │ Worker 池 ×4  │  │                │
│                     │  └───────┬───────┘  └───────┬───────┘  └───────────────┘  │                │
│                     └──────────┼──────────────────┼──────────────────────────────┘                │
│                                ▼                  ▼                                               │
│                        ┌──────────────┐   ┌──────────────┐                                        │
│                        │ RenderTarget │   │ RenderTarget │   ← 两张离屏画面                       │
│                        └──────┬───────┘   └───────┬──────┘                                        │
│                               └─────────┬─────────┘                                               │
│                                         ▼                                                         │
│                     ┌───────────────────────────────────────────┐                                 │
│                     │      过渡 Shader（postprocessing EffectPass）│                              │
│                     │  threshold = dist|uv  +  noise  +  mudNormal │                              │
│                     │  edge = progress - threshold                 │                              │
│                     │  blend = smoothstep(-fwidth(edge)·10, …)     │                              │
│                     │  edges = fwidth(luma) · mix(5, 10, progress) │  ← 线稿                       │
│                     │  glow  = smoothstep(0, thr, |edge|)          │  ← 边界发光                   │
│                     └───────────────────┬───────────────────────┘                                 │
│                                         ▼                                                         │
│                     ┌───────────────────────────────────────────┐                                 │
│                     │ Bloom(Luminance→MipmapBlur, 640×406) + SMAA │                              │
│                     └───────────────────┬───────────────────────┘                                 │
└─────────────────────────────────────────┼─────────────────────────────────────────────────────────┘
                                          ▼
┌────────────────────────────────── 输出层 ────────────────────────────────────────────────────────┐
│  <canvas>（sticky top-0 h-100vh -mb-100vh）        DOM 覆盖层（章节文案 / 导航 / 可访问性）        │
│  WebGL2 · ACESFilmic · pixelRatio 1.25 · 无阴影     NeueMontreal · H2 149.386px · tailwind v4    │
└──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 每帧的数据流（一帧内发生的全部事情）

```
Lenis.raf(t)
   └─▶ scroll 事件 → scrollY（插值后的浮点值）
         ├─▶ probe = scrollY + vh·0.3        → activeSection
         └─▶ computeSectionProgress(scrollY) → { current:{i,p}, next:{j,k} }
                    │
                    ├─▶ Scene i.applyTime(p)  → 相机 + 6 个图层的 transform
                    ├─▶ Scene j.applyTime(k)  → 同上（交叉溶解需要两张图同时在动）
                    │
                    ├─▶ render(Scene i) → rtCurrent
                    ├─▶ render(Scene j) → rtNext
                    ├─▶ TransitionPass(rtCurrent, rtNext, uProgress=p) → rtComposite
                    ├─▶ Bloom: 亮度提取 → 横模糊 → 竖模糊（半分辨率）
                    └─▶ Composite → 屏幕
```

---

## 5. 实现顺序（第十三阶段：从零做一个类似的站）

按这个顺序做，每一步都能独立看到结果，不会卡在半成品状态：

| 步 | 做什么 | 完成标志 |
|---:|---|---|
| 1 | **DOM 骨架 + sticky 画布**。先不写 WebGL，用 CSS 把 `sticky top-0 h-100vh -mb-100vh` 跑通，放一张纯色 canvas | 滚动时画布不动，章节叠在它上面滚 |
| 2 | **进度公式**。把 `computeSectionProgress` 落地，用调试面板打印 current/next | 首屏 `progress = vh/(h+vh)`，跨章时数值连续 |
| 3 | **单场景三层平面**。一个 Scene，bg/mid/fg 三个平面放在不同 z，透视相机 | 相机移动时三层视差可见 |
| 4 | **双场景 + 离屏纹理**。建两个 Scene，各自渲到 RenderTarget，中间用最简单的 `mix()` 合成 | 能看到两个场景按进度混合 |
| 5 | **换掉 mix()，接过渡 shader**。先只做阈值场，再加噪声/法线扰动 | 溶解边界是"活"的，不是硬边 |
| 6 | **加 fwidth 线稿 + 边界发光** | 过渡瞬间出现铅笔线稿与光晕 |
| 7 | **加后处理 bloom** | 高光溢到邻域像素 |
| 8 | **关键帧系统**。把硬编码的 transform 换成 config 里的轨道 | 改 config 就能改动画，不用碰组件 |
| 9 | **补降级路径**。WebGL2 能力检测 + 纯图 fallback | 关掉 WebGL 后页面仍可读 |

**关键：第 4 步之前不要碰 shader。** 先用 `mix()` 把管线跑通，确认两张纹理都对，
再替换成复杂 shader —— 否则 shader 报错时你分不清是管线问题还是 shader 问题。

---

## 6. 仍然无法确认的项（诚实边界）

| 项 | 状态 |
|---|---|
| 真实 draw call / GPU 帧耗时 / FPS | 浏览器公开环境无法确认 |
| `asset-2.frameProgress` 的确切语义 | 已排除 VideoTexture（asset-2 是 4 顶点 KTX2 平面） |
| `e0` / `e2` / `e3` 三层 shader 的完整源码 | 只拿到 `e1`（主过渡）分支 |
| `mudNormal` / `tNoise` 两张运行时匿名纹理对应的源文件 | 只能确认它们是独立贴图，未能匹配到具体 URL |
| 线上实际请求的是哪一份 GLB 版本 | 需抓真实运行时网络请求；CDN 上保留了迭代残留 |
| 过渡参数 `uProgress` 的确切来源 | 只提取到 `Tn()` 返回的 `{current, next}`，未找到把两者连起来的赋值行 |

---

## 7. 相关文件索引

```
reverse/shopify-winter2026/
├── 01-勘察报告.md                     页面结构 / 13 个 section 实测 / 技术栈确认表
├── 02-动画拆解.md                     13 场景 82 对象 / 375 轨道 / 780 关键帧全量
├── 03-深度还原.md                     progress 公式 / 运行时场景图 / 过渡 shader 全文 / 性能
├── 04-几何全量统计与最终架构.md        ← 本文
├── evidence/
│   ├── GLB_meta.json                  37 个 GLB 完整元数据（顶点/骨骼/动画/扩展/逐 mesh 明细）
│   ├── GLB_urls.txt                   37 个 GLB URL
│   ├── SHADER_transition.glsl         过渡 shader 完整源码（118 行）
│   ├── ASSETS_full.json               1247 个资产 URL
│   ├── page-source.html               原始 SSR HTML（1.45 MB）
│   └── theatre_summary.json           13 份 Theatre JSON 汇总
├── theatre/                           13 份 Theatre 项目状态 JSON
├── bundles/                           28 个 chunk
├── tools/
│   ├── fetch_glb_meta.py              ← GLB 元数据采集（Range 请求 + 解析 JSON chunk）
│   ├── probe_glb.py                   GLB 体积探测
│   └── gen_assets.py                  复刻 Demo 的占位素材生成
└── replica/                           可运行复刻 Demo
```
