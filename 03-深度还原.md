# 深度还原报告：动画系统 / 场景图 / Shader / 过渡机制

> 本文是 `01-勘察报告.md` 的续篇，同时**修正 01 中的 2 处错误**。
> 证据来源：13 份 Theatre 工程状态文件（已全部落盘 `theatre/`）+ 运行时 three.js devtools 钩子抓取 + bundle 源码切片。

---

## ⚠️ 0. 对 01 报告的两处更正

### 更正 1：three.js 版本不是 0.172.0，而是 **r181**

**01 报告错误**：我根据运行时请求的 `https://cdn.jsdelivr.net/npm/three@0.172.0/examples/jsm/libs/basis/basis_transcoder.js` 判定「three.js 0.172.0【已确认】」。

**实际证据**：bundle 内 three.js 源码头部就是版本常量——

```js
/** @license Copyright 2010-2025 Three.js Authors SPDX-License-Identifier: MIT */
const sa = "181", ...
```

而 `sa` 被 postprocessing 库引用为 `THREE_REVISION: sa.replace(/\D+/g,"")`，运行时读到的 `material.defines.THREE_REVISION === "181"`。

**结论**：**打包进站点的 three.js 是 r181**（2025 年版）。`three@0.172.0` 那个 URL 只是 `KTX2Loader.setTranscoderPath()` 被硬编码指向的一个 CDN 目录，**与主库版本无关**（可以是刻意钉版本，也可能是遗留未更新）。这是我用「依赖 URL 反推版本」这个方法的失误。

### 更正 2：移动端降级不是由视口宽度触发

**01 报告**沿用了「移动端默认 fallback」的说法。**实测推翻**：把视口缩到 502px（≤768 断点）重载后——

```
innerWidth 502 / canvasCount 5 / .canvas-wrapper 仍存在且 opacity:1
scrollHeight 59389（1021px 宽时是 43619 → 移动布局高 36%）
```

降级的真实触发条件是**能力检测**，不是宽度：

```js
function hasWebGL2() {
  try { return document.createElement("canvas").getContext("webgl2") !== null } catch { return false }
}
// 渲染分支
c === null ? null : c === false ? <FallbackImageStack/> : <div className="canvas-wrapper">
```

**即：拿不到 WebGL2 上下文才走纯图版**。另有 `useFallbackImages` 独立开关（疑似由 detect-gpu tier 0 驱动）。至于 `≤768` 那个判断，管的是别的逻辑（交互/滚动方式），不是 WebGL 开关。

另外我发现的 `*_mobile-fallback.png` 其实是**产品卡的小图版本**（`U47_Theme_index_on_mobile_mobile-fallback.png`），不是 WebGL 兜底图——兜底图是 `Fallback_Mobile_{Section}_2x.jpg`。

---

## 1. 滚动 → 进度 → 时间轴：完整公式（【已确认】）

### 1.1 progress 归一化（真实源码）

从 `(_locale).editions.winter2026-DhFtUF58.js` 提取，还原变量名后：

```js
function computeSectionProgress(scrollY, sectionMap, totalSections, vh = window.innerHeight) {
  if (totalSections === 0) return { current: { index: 0, progress: 0 }, next: null };

  let acc = 0;                                  // 累计高度 = 当前章节的顶部位置
  for (let i = 0; i < totalSections; i++) {
    const sec = sectionMap.get(i);
    if (!sec) continue;
    const h    = sec.height;
    const ec   = vh * (sec.earlyCrossfade ?? 0); // 提前交叉量
    const zone = h + vh + ec;                    // 区间总长
    const start = acc - vh - ec;                 // 区间起点（比章节顶部早一个视口）
    const end   = acc + h;                       // 章节底部

    if (scrollY < end) {
      const progress = clamp01((scrollY - start) / zone);
      const current  = { index: i, progress };

      // ★ 同时算出「下一章」的进度，用于双纹理交叉淡入
      let next = null;
      if (i + 1 < totalSections) {
        const nx = sectionMap.get(i + 1);
        if (nx) {
          const nEc = vh * (nx.earlyCrossfade ?? 0);
          const nZone = nx.height + vh + nEc;
          const nStart = end - vh - nEc;
          const k = clamp01((scrollY - nStart) / nZone);
          if (k > 0) next = { index: i + 1, progress: k };
        }
      }
      return { current, next };
    }
    acc += h;
  }
  return { current: { index: totalSections - 1, progress: 1 }, next: null };
}
```

**核心公式**：

```
progress_i = clamp01( (scrollY − (sectionTop_i − vh − ec_i)) / (sectionHeight_i + vh + ec_i) )

其中 sectionTop_i = Σ(height_0 … height_{i−1})
     ec_i = vh × earlyCrossfade_i
```

**`earlyCrossfade` 的值**（源码中只有 3 处出现，第 3 处是关键）：

```js
earlyCrossfade: E >= 2 ? 0.2 : 0     // E = 章节 index
```

→ **前两章（Hero、Sidekick）earlyCrossfade = 0；从第 3 章起 = 0.2**，即从第三章开始提前 **20% 视口高**启动交叉过渡。这条第三方文档的结论**复核通过**。

### 1.2 progress → sequence.position

```js
scale  = (sectionHeight + vh) / vh
offset = (sectionIndex === 0) ? introOffset : 0     // hero 序曲期 introOffset ∈ [-1, 0]
sequence.position = Math.max(0, progress * scale + offset)
```

阻尼路径：`position += (target − position) × 0.5` 每帧（仅当 `|activeSection − i| ≤ 1`）。
直连路径：订阅到 progress 变化时**直接赋值**（无阻尼）。

> 两条路径在源码里同时存在（一个 `useEffect` 走 rAF 阻尼、一个走直连订阅），这也解释了为什么运行时采样时 fov 与 position.z 会呈现不同的跟随速度。

### 1.3 运行时实测验证（不是推测，是测出来的）

用 three.js devtools 钩子抓取真实相机，滚动到不同位置读取参数：

| scrollY | Hero 相机 z | Hero fov | 解读 |
|---|---|---|---|
| 0 | 6.439 | 25.00 | = 关键帧 position 1.0 的值（6.45 / fov 25）**完全吻合** |
| 200 | 7.538 | 24.82 | position ≈ 1.08 |
| 400 | 9.985 | 24.15 | position ≈ 1.78（接近关键帧 1.8 的 z=10.34） |
| 600 | 9.942 | 23.05 | position ≈ 2.36 |
| 800 | 4.090 | 22.27 | position ≈ 2.55（fov 已接近关键帧 2.6 的 22） |
| 1000 | −18.577 | 25.00 | **回到关键帧 position 0**（Hero 交出控制权） |
| 1200+ | −18.577 | 25.00 | 冻结 |

**这组数据的意义**：
1. **fov 真的在动**（25 → 22.27）→ Theatre 的 fov 轨道是活的，不是死数据。
2. **position.z 真的按关键帧曲线走**（6.45 → 10.34 → 回落）→ 曲线形状与 JSON 完全一致。
3. **Hero 的序列只走到 ≈2.5**，从未到达 2.667 —— 因为 `scale = (971+650)/650 = 2.494`，而 progress 上限为 1。**Hero 的关键帧 2.667 之后的部分在正常滚动中永远不会播到**（可能是为跳章/直达预留的）。
4. scrollY ≥ 1000 后 Hero 相机回到 position 0 并冻结 → 因为它已不是 active 场景，sticky 画布已切换到 Sidekick 场景，视觉上无影响。

**同时验证到的相机参数（运行时真值）**：

| 场景 | 类型 | fov | near | far | aspect |
|---|---|---|---|---|---|
| Hero | PerspectiveCamera | 25（轨道 25→22） | **0.1** | **1000** | 1.5751 |
| Sidekick | PerspectiveCamera | 30 | **0.1** | **1000** | 1.5751 |

---

## 2. 13 场景动画全量拆解（第四阶段）

完整逐场景表格见 **`02-动画拆解.md`**（45 354 字符，含每个属性的全部关键帧原始值）。这里只给总览与要点。

| 场景 | 序列长 | 对象数 | 轨道数 | 关键帧数 | 对象 |
|---|---|---|---|---|---|
| Hero | 10 | 10 | 33 | 87 | cube, asset-1..3, camera, light-1, landscape, bloom-effect, effects, particles |
| Sidekick | **12.48** | 11 | 41 | 87 | cube, asset-1..3, stars, camera, particles, background, environment, white-overlay, effects |
| Retail | **9.54** | 10 | 49 | **115** | cube, asset-1..4, camera, retail-splat, environment, bloom-effect, effects |
| Developer | 10 | 7 | 45 | 83 | cube, asset-1, asset-3, camera, light-1, scan-key, key |
| Online | 10 | 9 | 30 | 77 | asset-1..3, camera, sidekick-background, online-background, code, white-overlay, effects |
| Agentic | 10 | 7 | 39 | 65 | camera, asset-1..4, key, effects |
| Marketing | 10 | 6 | 27 | 60 | cube, blur-intensity, camera, billboard, effects, asset-3 |
| Operations | 10 | 6 | 27 | 53 | cube, asset-1, asset-3, camera, light-1, effects |
| Shipping | 10 | 5 | 25 | 42 | camera, asset-1..3, key |
| Checkout | 10 | 3 | 19 | 34 | asset-1, camera, effects |
| ShopApp | 10 | 2 | 14 | 27 | asset-1, camera |
| B2B | 10 | 4 | 14 | 26 | camera, asset-1, asset-2, light-1 |
| Finance | 10 | 2 | 12 | 24 | camera, asset-1 |
| **合计** | — | **82** | **375** | **780** | — |

**要点**：

- **序列长度不全是 10**：Sidekick 是 **12.48**、Retail 是 **9.54**。说明序列长度是设计上按需设的，不是统一值。`subUnitsPerUnit` 全部为 30。
- **Hero 是唯一没有 `environment` 对象但有 `landscape` 的场景**。
- **`light-1` 只出现在 5 个场景**（Hero / B2B / Developer / Operations / Sidekick），且 Hero 里 `intensity: 0`、`display: false` —— **红蓝补光实际是关闭的**。
- **`key` 对象出现在 4 个场景**（Agentic / Developer / Shipping + key.glb）→ 钥匙彩蛋是跨场景叙事。
- **`cube` 是通用占位/调试对象**，出现在 7 个场景，Hero 里 rotation 有值（0.35/0.5/0.47）但不可见。

**Hero 相机完整关键帧**（原始值，`position → value`）：

| 属性 | 关键帧 |
|---|---|
| `position.x` | 0→0  1→0  1.8→0  2.667→0 |
| `position.y` | 0→0  1→0  1.8→**0.74**  2.667→−0.21 |
| `position.z` | 0→**−18.57**  1→**6.45**  1.8→**10.34**  2.667→**−2.29** |
| `rotation.x` | 0→0  1→0  1.8→0  3.033→−0.04 |
| `rotation.y` | 0→0  1→0  1.8→0  3.033→3.47e−18 |
| `rotation.z` | 0→**0.27**  1→0  1.8→−0.025  3.033→**0.28** |
| `fov` | 1→**25**  2.6→**22** |
| `target.x` | 0.267→0  1→0  1.8→0  2.667→0 |
| `target.y` | 0.267→0  1→0  1.8→**0.54**  2.667→0 |
| `target.z` | 0.267→**−21.8**  1→1  1.8→1  2.667→**−7.06** |

**静态覆盖（Hero，完整）**：

| 对象 | 参数 |
|---|---|
| `asset-1` | scale (2,2,3) · position (−0.233, 0.141, **−7.09**) · rotation.y 0.07 · **roughness 0** · autoplay false |
| `asset-2` | scale **12** · position z **−25.46** · rotation.x **3.16** · `frameProgress 0.72` · `responsive "cover"` |
| `asset-3` | scale **4** · position (−0.27, 0.97, **−5.42**) · rotation (−1.39, 6.30, 0.03) |
| `landscape` | scale **20** · position z −20 · rotation.z 3.136 · opacity 0.98 |
| `particles` | **count 500** · size 0.25 · speed 0.5 · turbulence 0.5 · scale 15 · position z **−12.29** · color **rgb(1, 0.886, 0.549)** · opacity 0.59 · blink{min 0.19, speed 2.2} · pulse{speed 2} |
| `butterflies` | **enabled false** · count 6 · scale 0.06 · color rgb(0.561, 0.776, 0.898) · glowStrength 1.7 · **wanderStrength 0.5 / separationRadius 4 / boundaryRadius 10**（boids） · hue 2.608 · movementSpeed 2 · opacity 0.81 |
| `camera` | `pan {min −0.05, max 0.05}` · `tilt {min −0.02, max 0.02}` |
| `effects` | `bloom.intensity 4.8` · `sectionTransition.fadeCenter (−0.08, 0.79, −7.5)` |
| `bloom-effect` | intensity 3 |
| `light-1` | **display false** · intensity 0 · position (0.2, 0, −0.55) · distance 0 |
| `light-2` | intensity 0 |

**关键帧缓动**：每个关键帧带 `type:"bezier"` + `handles:[x1,y1,x2,y2]`（如 `[0.5,1,0.55,0.085]`、`[0.401,0.623,0.448,0.403]`）——**逐帧手调，不是预设曲线**。这是「高级感」的直接来源之一。

---

## 3. WebGL 场景图（第五阶段，运行时真实抓取）

用 `window.__THREE_DEVTOOLS__` 钩子在页面加载前注入，three.js 会把 renderer / scene / Object3D 逐个交出来。**以下是运行时真值，不是推测。**

### 3.1 Renderer

| 项 | 值 |
|---|---|
| pixelRatio | **1.25** |
| toneMapping | **4 = ACESFilmicToneMapping** |
| outputColorSpace | `srgb` |
| shadowMap | **false（完全关闭）** |
| drawingBuffer | 1279 × 812（CSS 1023 × 650） |
| geometries / textures / programs | **18 / 39 / 14** |
| 捕获到的 Scene 数 | **11**（= 1 主场景 + 10 个后处理 pass 的临时 Scene） |

### 3.2 Hero 场景图（真实层级）

```
Scene
├── PerspectiveCamera   pos(0.25, −0.249, 6.439)  rot(0.046, 0.046, −0.002)  fov 25  near 0.1  far 1000
├── Points              pos(0, 0, −12.293)  scale 15
│     geometry: 1000 verts, attrs [position, velocity, phase, sizeScale]
│     material: ShaderMaterial  uniforms [uSize, uSizeVariation, uColor, uOpacity,
│               uBlur, uTime, uSpeed, uTurbulence, uBlinkSpeed, uBlinkMin, tNoise]
│     transparent, blending=2(AdditiveBlending), depthWrite=false
├── Group  pos(−0.233, 0.141, −7.09)  scale(2, 2, 3)          ← asset-1
│   └── Group "EW26_Hero_251207v3_compressed-optimized"        ← 这就是那个 GLB
│       ├── Object3D "Robed_Woman_Rig"  pos(−0.989, 0.177, −0.059) scale 1.193 rot.z 0.106
│       │   ├── SkinnedMesh "Robed_Woman"   7 090 verts / 23 958 idx
│       │   │      attrs [position, normal, uv, skinIndex, skinWeight]  ← 骨骼蒙皮
│       │   │      material MeshStandardMaterial  metalness 0  roughness 1
│       │   │      map "Hero_Packed_Texture.psd"  2048×2048  KTX2/BC7
│       │   ├── Object3D "Skateboard_Mover"  rot(2.817, −0.323, −2.957)
│       │   ├── SkinnedMesh "Woman_Head"    678 verts / 3 666 idx   map "Hero_fg_Heads" 1024×512
│       │   └── Bone
│       └── Object3D "Robed_Man_Rig"  pos(1.621, −0.695, −0.477) scale 0.326
│           ├── SkinnedMesh "Man_Head"      604 verts / 3 309 idx   map "Hero_fg_Heads" 1024×512
│           ├── SkinnedMesh "RobedMan"     12 555 verts / 55 938 idx  map "Hero_Packed_Texture.psd"
│           └── Bone_1
└── Group  pos(0, 0, −25.46)  scale 18.901  rot(3.16, 0, 0)    ← asset-2
    └── Mesh   4 verts / 6 idx（一个平面）  scale(1, 0.699, 1)
          material MeshStandardMaterial  side=2(DoubleSide)  transparent
          map 2048×1432  KTX2/BC7
```

### 3.3 Sidekick 场景图

```
Scene
├── Points  pos(0, 0, −0.3)  scale(4, 4, 5)  1000 verts
├── PerspectiveCamera  pos(0.132, 0.413, 2.469)  fov 30
├── Group  pos(−0.07, 0.062, 0)  rot(0.007, −0.021, −0.383)    ← asset-1
│   └── Group "EW26_Sidekick_251208_compressed-optimize"
│       ├── Mesh "Sidekick_laptop"      9 442 verts / 53 700 idx  map "Sidekick_fg_diffuse_clean" 2048²
│       ├── Mesh "sidekick-eyelids"       163 verts / 777 idx
│       ├── Mesh "shiny-ring1"          2 570 verts   MeshPhysicalMaterial metalness 1 roughness 0
│       ├── Mesh "shiny-ring2"            721 verts
│       ├── Mesh "shiny-ring4"            703 verts
│       ├── Mesh "shiny-ring5"            588 verts
│       └── Mesh "shiny-ring6"          2 940 verts
└── Group  pos(−5.481, 0, −18.84)  scale 12.601  rot(3.16, 0, 0)   ← asset-2（背景平面）
    └── Mesh  4 verts  map 2048×1460
```

### 3.4 三个颠覆性结论

**① 人物是真正的骨骼蒙皮网格，不是 2D 分层贴图。**
`Robed_Woman` / `RobedMan` 是 `SkinnedMesh`，顶点属性含 `skinIndex` + `skinWeight`，场景里有 `Bone` 节点。**这直接推翻了我 01 报告里「前景是独立 GLB 平面层」的暗示**——前景人物是带骨骼的 3D 模型（`Robed_Woman_Rig` / `Robed_Man_Rig` 是两个独立 rig，各自有 scale 与 rotation 偏移）。

**② `asset-2` 确实是 4 顶点平面** —— 这就是「背景层」，贴图 2048×1432 的 KTX2，`DoubleSide` + `transparent`，`rotation.x = 3.16 ≈ π`（翻转朝内）。所以架构是「**3D 人物模型 + 2D 背景平面 + 点云粒子**」的混合，不是纯 3D 也不是纯 2D。

**③ `frameProgress` 不是视频纹理。** 运行时读到的 `asset-2` 贴图：`isVideoTexture: false`、`isCompressedTexture: true`、`format 36492`（= `RGBA_BPTC_Format` / BC7）。**浏览器公开环境已确认它不是 VideoTexture**；至于 `frameProgress` 是否用于 KTX2 内的帧序列 UV 偏移，**仅凭公开环境无法确认**（需要读到 shader 里对该 uniform 的用法）。

### 3.5 纹理格式真相

全部场景贴图：`isCompressedTexture: true`、`format 36492`（BC7）、`colorSpace: 'srgb'`、`minFilter 1008`（LinearMipmapLinear）、`magFilter 1006`（Linear）、`anisotropy 1`、`generateMipmaps false`、`flipY false`。

→ **KTX2 里装的是 BC7（BPTC）块压缩，解码后直接上传 GPU，不生成 mipmap、不做各向异性过滤。** `flipY: false` 是 KTX2Loader 的默认行为（KTX2 规范自带正确的 UV 朝向）。

---

## 4. 后处理链与过渡机制（第六 + 七阶段）——本次最大发现

### 4.1 后处理渲染目标链（运行时实测）

| Render Target | 尺寸 | 说明 |
|---|---|---|
| `EffectComposer.Buffer` | 1279 × 812 | 主颜色缓冲（= drawingBuffer 尺寸） |
| `EffectComposer.StableDepth` | — | 稳定深度缓冲（format 1026 = DepthStencil） |
| `SMAA.Edges` | 1279 × 812 | SMAA 边缘检测（全分辨率） |
| `SMAA.Weights` | 1279 × 812 | SMAA 权重 |
| `SMAA.Search` | 64 × 16 | SMAA 内置查找表 |
| `SMAA.Area` | 160 × 560 | SMAA 内置面积图 |
| `Downsampling.Mipmap0` | 640 × 406 | **Bloom 降采样第一级** |
| `Upsampling.Mipmap1` | 320 × 203 | Bloom 上采样（更粗一级） |
| `Upsampling.Mipmap0` | 640 × 406 | Bloom 上采样 |
| 匿名 256×256 | 256 × 256 | `tNoise` 噪声纹理（DataTexture，未压缩） |
| 匿名 512×512 | 512 × 512 | 过渡用中间渲染目标 |

**注意**：Bloom 只降到 640×406（约 1/2 分辨率）就开始上采样 —— **不是标准的多级 mipmap bloom**，层数很少。这解释了它为什么便宜。

### 4.2 后处理 pass 清单（从 pass 的 uniforms/defines 反推，【已确认】）

| pass | 识别依据（uniforms / defines） |
|---|---|
| **LuminancePass**（Bloom 第一级） | `[inputBuffer, threshold, smoothing, range]` + `THRESHOLD, COLOR` |
| **MipmapBlurPass / KawaseBlurPass** | `[inputBuffer, supportBuffer, texelSize, radius]` |
| **SMAAEffect**（带深度边缘检测） | `[inputBuffer, depthBuffer, predicationBuffer, texelSize]` + `LOCAL_CONTRAST_ADAPTATION_FACTOR 2.0`、`EDGE_THRESHOLD 0.1`、`DEPTH_THRESHOLD 0.01`、`PREDICATION_MODE 0`、`DEPTH_PACKING 3200`、`EDGE_DETECTION_MODE 2` |
| **SMAAEffect 第二 pass** | `[inputBuffer, searchTexture, areaTexture, resolution, texelSize]` + `MAX_SEARCH_STEPS 16`、`AREATEX_MAX_DISTANCE 16` |
| **CopyPass / 输出** | `[inputBuffer, depthBuffer, channelWeights, opacity]` + `COLOR_SPACE_CONVERSION, DEPTH_PACKING 0, COLOR_WRITE` |
| **自定义 TransitionEffect（`e0/e1/e2/e3` 四层）** | 见 4.3 |

**注意**：SMAA 用 `DEPTH_PACKING 3200`（= RGBADepthPacking）+ `EDGE_DETECTION_MODE 2`（depth-based）→ **它用深度缓冲做边缘检测**，比纯 luma 边缘更准。这也是为什么它敢只用 SMAA 而不用 MSAA。

### 4.3 自定义过渡 Effect 的 uniform 全清单（【已确认】）

```
inputBuffer, depthBuffer, resolution, texelSize, cameraNear, cameraFar, aspect, time,
e0WeightMap, e0BlendOpacity,
e1TCurrent, e1TNext, e1TMudNormal, e1TNoise, e1UProgress, e1UAspect, e1UTime,
e1UResolution, e1UMouse, e1UIsHero, e1UIsFallback, e1UProjectionView,
e1UFadeCenterPoint, e1UDarken, e1BlendOpacity,
e2TMudNormal, e2TNoise, e2UPosition, e2UTime, e2UColor, e2UResolution, e2BlendOpacity,
e3TCurrent, e3THatMask, e3TMudNormal, e3TNoise, e3UInvertedSobelActive, e3UHatHover,
e3UModalAnimation, e3UIsModalAnimating, e3UInvertedSobelColor,
e3UInvertedSobelFadeCenter, e3UResolution, e3UTime, e3BlendOpacity
```

**这是整个网站视觉的架构核心。** 四层效果栈 `e0 → e3`，每层有独立的 `BlendOpacity` 与专属纹理：

- **`e1` = 场景切换层**：`e1TCurrent`（当前场景渲染结果）+ `e1TNext`（下一场景渲染结果）+ `e1UProgress`（过渡进度）+ `e1UFadeCenterPoint`（3D 空间的过渡中心点，**就是 Theatre 里的 `effects.sectionTransition.fadeCenter`**）+ `e1UDarken` + `e1UMouse` + `e1UProjectionView` + `e1UIsHero` / `e1UIsFallback`
- **`e2` = 另一个过渡层**（有 `e2UPosition` / `e2UColor`）
- **`e3` = 帽子/模态层**：`e3THatMask` + `e3UHatHover` + `e3UInvertedSobelActive` + `e3UInvertedSobelColor` + `e3UModalAnimation` → **鼠标悬停帽子时的 Sobel 反色效果**
- **`e0` = 权重图**（`e0WeightMap`）

### 4.4 过渡 Shader 完整源码（【已确认】，4711 字符，已落盘 `evidence/SHADER_transition.glsl`）

```glsl
uniform sampler2D tCurrent;
uniform sampler2D tNext;
uniform sampler2D tMudNormal;
uniform sampler2D tNoise;
uniform float uProgress;
uniform float uAspect;
uniform float uTime;
uniform vec2  uResolution;
uniform vec2  uMouse;
uniform float uIsHero;
uniform float uIsFallback;
uniform mat4  uProjectionView;
uniform vec3  uFadeCenterPoint;
uniform float uDarken;

float sampleNoise(vec2 uv) { return texture(tNoise, uv).r * 2.0 - 1.0; }

float easeInOutCubic(float t) {
  return t < 0.5 ? 4.0 * t * t * t : 1.0 - pow(-2.0 * t + 2.0, 3.0) / 2.0;
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
  bool isHero     = uIsHero > 0.5;
  bool isFallback = uIsFallback > 0.5;

  // ① Hero 的过渡进度用 smoothstep(0, 1.5) 压缩 —— Hero 的过渡比别的场景慢
  float progress = isHero ? smoothstep(0.0, 1.5, uProgress) : uProgress;

  // ② 把 3D 空间的 fadeCenterPoint 投影到屏幕空间，作为过渡中心
  vec2 sceneCenter;
  if (isFallback) sceneCenter = vec2(0.5, 0.65);
  else {
    vec4 clipPos = uProjectionView * vec4(uFadeCenterPoint, 1.0);
    sceneCenter = (clipPos.xy / clipPos.w) * 0.5 + 0.5;
  }

  // ③ Hero 独有：围绕 3D 中心做 10% 的推拉变焦
  vec2 currentUV = uv, nextUV = uv;
  if (isHero) {
    currentUV = (uv - sceneCenter) * (1.0 - smoothstep(0.2, 1.0, uProgress) * 0.1) + sceneCenter;
    nextUV    = (uv - sceneCenter) * (1.0 + smoothstep(0.8, 0.0, uProgress) * 0.1) + sceneCenter;
  } else if (isFallback) {
    currentUV.y -= easeInOutCubic(uProgress) * 0.1;
    nextUV.y    += easeInOutCubic(1.0 - uProgress) * 0.1;
  }

  vec4 current = texture(tCurrent, currentUV);
  vec4 next    = texture(tNext,    nextUV);

  // ④ 用硬件导数 fwidth 做边缘检测 —— 过渡时把画面变成「线稿」
  vec3 currentEdges = vec3(0.0), nextEdges = vec3(0.0);
  if (uProgress > 0.01 && uProgress < 0.99 || uDarken > 0.001) {
    float currentLuma = dot(current.rgb, vec3(0.299, 0.587, 0.114));
    currentEdges = vec3(fwidth(currentLuma) * mix(5.0, 10.0, progress));
    float nextLuma = dot(next.rgb, vec3(0.299, 0.587, 0.114));
    nextEdges = vec3(fwidth(nextLuma) * mix(5.0, 10.0, 1.0 - progress));
  }

  // ⑤ 泥土法线 + 噪声，扰动过渡边界（"displacement" 的真身）
  vec3  mudNormal  = texture(tMudNormal, uv * 2.0).rgb;
  float mudStrength = isHero ? mix(0.2, 0.4, 0.5 + 0.5 * sin(uTime - uv.x * 10.0))
                             : mix(0.3, 0.6, 0.5 + 0.5 * sin(uTime - uv.x * 10.0));
  float mudOffset  = (mudNormal.r - 0.5) * mudStrength;

  vec2  aspectUv    = vec2(uv.x * uAspect, uv.y) + vec2(0.0, uTime * 0.02);
  float noiseSpeed  = isHero ? 0.07 : 0.05;
  float currentNoise = sampleNoise(aspectUv * 0.25 - uTime * noiseSpeed * 0.125);

  vec2 maskCenter = isHero ? mix(sceneCenter, uMouse, 0.1) : vec2(0.5);

  // ⑥ 阈值场：Hero 是「圆形揭示」，其他是「斜向擦除」
  float threshold;
  if (isHero) {
    float dist = length((uv - maskCenter) * vec2(uAspect, 1.0)) * 0.8;   // 按宽高比修正成圆
    threshold = mix(dist, uv.x,
                    smoothstep(0.6, -0.4, abs(uv.x - sceneCenter.x))
                    * mix(0.0, 0.4, smoothstep(0.05, 0.5, progress)));
    threshold = mix(threshold, 0.0, smoothstep(0.9, 1.0, uProgress));
  } else {
    float ease = mix(progress * progress * (3.0 - 2.0 * progress), progress, 0.25);
    threshold = mix(uv.y, uv.x, smoothstep(0.6, -0.4, abs(uv.x - 0.5)) * 0.5);
    progress = ease;
  }
  threshold = threshold * 2.0 - 1.0;
  threshold = threshold / 1.2 + currentNoise * 0.2 + mudOffset;   // ← 噪声 + 法线扰动
  threshold = threshold * 0.5 + 0.5;

  float edge = progress - threshold;
  float aa   = fwidth(edge) * 10.0;                              // 导数抗锯齿
  float blendFactor = smoothstep(-aa, aa, edge);

  // ⑦ 交叉时把两侧都推向线稿
  current = mix(current, vec4(currentEdges, 1.0), smoothstep(0.0, 0.5, progress));
  next    = mix(next,    vec4(nextEdges,    1.0), smoothstep(0.2, 0.8, 1.0 - progress));

  if (uDarken > 0.001) {
    current = mix(current, vec4(0.02 * uDarken + currentEdges * 0.1, 1.0), uDarken);
  }

  outputColor = mix(current, next, blendFactor);

  // ⑧ 边界发光带
  float glowStrength = isHero ? mix(40.0, 8.0, smoothstep(0.05, 0.25, progress)) : mix(2.0, 10.0, 0.5);
  float glowThreshold = isHero ? glowStrength * 0.001 : 0.003;
  float glowFactor = smoothstep(0.0, glowThreshold, abs(edge));
  float glowMult = isHero
    ? mix(glowStrength * 0.5, glowStrength, 0.5 + 0.5 * currentNoise * sin(uTime + uv.x * 10.0))
    : mix(2.0, 10.0, 0.5 + 0.5 * currentNoise * sin(uTime + uv.x * 10.0));
  outputColor = mix(outputColor, outputColor * glowMult, 1.0 - glowFactor);
}
```

### 4.5 过渡机制总结（这是「它为什么看起来那么贵」的答案）

它**不是**位移贴图（displacement map），**不是** dissolve 噪声溶解，**不是** chromatic aberration。它是：

**「双场景纹理 + 阈值场 + 噪声/法线扰动 + 硬件导数线稿 + 边界发光」的五合一。**

1. **两个场景各自渲染到独立 render target**（`tCurrent` / `tNext`），由 `computeSectionProgress` 同时算出的 `current` 与 `next` 驱动——**这就是为什么它能在滚动中途做交叉过渡，而不是章节切换时才切**。
2. **过渡边界 = 一个阈值场**：Hero 是按宽高比修正的**圆形揭示**（圆心是 3D 空间某个点投影到屏幕的位置，`fadeCenterPoint` 由 Theatre 驱动），其他章节是**斜向擦除**（擦除角度随 `|uv.x − 0.5|` 变化，形成弧形而非直线）。
3. **边界被两层东西扰动**：一张 `mudNormal`（泥土法线，`uv * 2.0` 采样）+ 一张滚动噪声（`aspectUv * 0.25 − uTime * noiseSpeed * 0.125`），再叠一个 `sin(uTime − uv.x * 10.0)` 的横向波动。**所以边界是活的、有机的、呼吸的**。
4. **过渡时画面变成线稿**：`fwidth(luma) * 5~10` 是硬件导数边缘检测，放大 5–10 倍后混进原图 —— **出画变线稿、入画从线稿还原**。
5. **边界上有发光带**：`glowStrength` Hero 是 `mix(40, 8, ...)`，乘上噪声与正弦调制 —— 一条会闪烁的亮边。
6. **抗锯齿也用导数**：`aa = fwidth(edge) * 10` → 不需要额外 pass。

---

## 5. 设计 token（第九阶段，从 `tailwind.css` 实测）

Tailwind v4.2.2，单文件 129 725 B。字体三个：

| 字体 | 用途 | 来源 |
|---|---|---|
| **NeueMontreal** | 正文与标题（几何无衬线） | `cdn.shopify.com/b/shopify-brochure2-assets/{md5}.woff2` ×2（preload） |
| **HWCigars** | 手写体强调（如 "Complexity, delegated"） | 同上 |
| **ImperialScript** | 花体首字下沉 / 装饰 | 同上 |

**字号阶梯（实测 `getComputedStyle`）**：

| 元素 | font-size | weight | letter-spacing | line-height | 字族 |
|---|---|---|---|---|---|
| H2 章节标题 | **149.386px** | 700 | **−4.48158px** | 134.447px | NeueMontreal |
| H3 小节标题 | 32px | 700 | −0.96px | 30.4px | NeueMontreal |
| H3 手写 | 36.7018px | 400 | −1.83509px | 35.2337px | **HWCigars** |
| H3 产品卡标题 | 14px | 700 | normal | 15.4px | NeueMontreal |
| 大号数字/年份 | 56.9386px | 700 | −1.13877px | 51.2448px | NeueMontreal |

**关键规律**：
- **标题字号与字距是 clamp/vw 驱动的浮点数**（149.386px 这种精度说明是 `clamp()` 或 `vw` 计算值，不是固定 px）。
- **letter-spacing 与 font-size 成比例**：−4.48/149.4 = **−0.03em**；−0.96/32 = **−0.03em**；−1.14/56.9 = **−0.02em**。→ **大标题统一约 −0.03em 收紧**。
- **line-height < font-size**（134.4/149.4 = **0.9**）→ 行高压得比字还紧，这是「海报感」的关键。
- 字号跳跃极大（149 → 32 → 14），**中间档位故意缺失** → 强层级对比。

**已知缓动曲线**（第三方文档给出，我未能独立验证 → 标注【高度可能】）：

```css
--ease-back:  cubic-bezier(.34, 1.56, .64, 1)
--ease-frame: cubic-bezier(.72, .16, .19, .96)
--ease-lines: cubic-bezier(.34, .22, .47, .84)
--ease-fade:  cubic-bezier(.25, .46, .45, .94)
```

**色彩**（01 报告已确认）：`body` 背景 `rgb(0,0,0)`，light 状态与幕布色 `#dcdcd0` 系衔接；主题由 `data-nav-theme` / `data-sidebar-theme` / `data-title-theme` / `data-nav-bg` 四个 html 属性驱动，三者独立阈值。

---

## 6. 性能（第十阶段）

| 项 | 实测值 / 机制 |
|---|---|
| 首屏 drawingBuffer | 1279 × 812（DPR 1.25） |
| 首屏几何 / 纹理 / program | **18 / 39 / 14** |
| 主渲染目标 | 全分辨率（无降分辨率） |
| Bloom | 降到 **640×406** 后即上采样（约 1/2 分辨率，层级很少） |
| SMAA | 全分辨率 + 深度边缘检测 |
| 阴影 | **完全关闭** |
| toneMapping | ACESFilmic（在 shader 里做，不是额外 pass） |
| 顶点量级（Hero） | Robed_Woman 7 090 + RobedMan 12 555 + 2 个头部 ~1 282 + 粒子 1 000 + 背景 4 ≈ **22 K 顶点** |
| 三角面量级（Hero） | 23 958 + 55 938 + 3 666 + 3 309 ≈ **87 K 三角** |
| 纹理 | 全部 KTX2/BC7，2048² 为主，**不生成 mipmap、各向异性 1** |
| 场景挂载 | 懒挂载，canvas 数随滚动 1 → 4 → 5 |
| 内容可见性 | `content-visibility` + `contain-intrinsic-size` |
| 降级 | WebGL2 能力检测 → 纯图版；detect-gpu tier 分档；`prefers-reduced-motion` 时 lerp 系数置 1（动画变瞬移） |
| Worker | 4 池（Draco / KTX2 解码） |
| 性能埋点 | 代码里向分析平台上报 `parentName:"renderer_pipeline"` + 每帧 `duration` |

### 复刻时最吃性能的地方（结论）

1. **全分辨率 + 全分辨率的后处理链**：`EffectComposer.Buffer`、`SMAA.Edges`、`SMAA.Weights` 都是 1279×812。DPR 1.25 时这是 3 张全分辨率缓冲。**这是最大的开销**，也是原站愿意付的代价（画质优先）。
2. **过渡时同时渲染两个场景**：`tCurrent` + `tNext` → 过渡期间 **draw call 翻倍**。这就是为什么它必须限制「只驱动 activeSection ± 1」。
3. **约 87 K 三角 + 7 个 SkinnedMesh**：骨骼蒙皮每帧要重算，SkinnedMesh 数量多比顶点数更贵。
4. **KTX2 解码**：BC7 纹理解码 + Worker 池，首屏如果不同步会卡；原站用流式解码解决。
5. **`fwidth` 边缘检测**：在过渡期间对两张全分辨率纹理做导数运算 —— 只在过渡时开启（源码里有 `if (uProgress > 0.01 && uProgress < 0.99)` 守卫），**这个守卫很关键，复刻时必须照抄**。

---

## 7. 证据文件索引（新增）

```
reverse/shopify-winter2026/
├── 01-勘察报告.md                  第一阶段（含 2 处待更正结论，见本文第 0 节）
├── 02-动画拆解.md                  13 场景逐属性全量关键帧（45 KB）
├── 03-深度还原.md                  本文
├── theatre/                        13 份 Theatre 工程状态原始 JSON（~470 KB）
├── evidence/
│   ├── SHADER_transition.glsl      ★ 4711 字符完整过渡 shader
│   ├── X3_GLSL_samples.txt         4 段粒子/蝴蝶 shader
│   ├── theatre_summary.json        程序化解析出的场景/对象/轨道汇总
│   ├── ASSETS_full.json            1247 个资产 URL
│   └── page-source.html            页面原始 HTML（1.45 MB）
└── bundles/                        26 个 chunk（含 three r181 + postprocessing）
```

---

## 8. 仍然无法确认的项（诚实边界）

1. **真实 draw call 数 / GPU 帧耗时 / 实际 FPS** —— 需要 GPU 计时器或 Performance trace，公开环境读不到。
2. **`asset-2.frameProgress` 的确切语义** —— 已确认不是 VideoTexture，但是否为 KTX2 内帧序列索引，需要读到 shader 里该 uniform 的用法。
3. **`e0` / `e2` / `e3` 三层的完整 shader** —— 我只拿到了 `e1`（场景过渡）那一支的源码；`e3` 的 Sobel 反色效果从 uniform 名可推断用途，但没有源码。
4. **各章节 GLB 的完整顶点统计** —— 我只抓到了首屏加载的 Hero + Sidekick 两个场景（懒挂载机制决定了其余 11 个场景要滚到才会实例化）。
5. **`mudNormal` / `tNoise` 两张纹理的具体来源文件** —— 运行时是匿名纹理，未与 URL 对应上（候选：`mud_normal.webp`、`packed_dirt_normal_map.jpg`）。
